function Func(p1)
{
    var _a = 1;   // start with an underscore
    var $b = 1;   // start with a dollar-sign (why?)
    var C = 1;    // starts with capital letter
    var d = 1;    // starts with lower-case letter
    var 你好 = 2; // starts with UNICODE character
    
    \u4f60\u597d += 18; // use the escaped name (should come out the same as the non-escaped above)
}